model Achievement {
  id          String   @id @default(cuid())

  title       String
  description String?

  issuer      String?

  image       String?

  achievedAt  DateTime?

  createdAt   DateTime @default(now())
}