model VisitorAnalytics {
  id          String   @id @default(cuid())

  ipAddress   String?
  userAgent   String?

  country     String?
  city        String?

  page        String

  createdAt   DateTime @default(now())

  @@index([page])
  @@index([createdAt])
}