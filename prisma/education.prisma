model Education {
  id            String   @id @default(cuid())

  institute     String
  degree        String
  field         String?

  startYear     Int
  endYear       Int?

  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}